**About**

This is the offine version of the Android app that runs entirely local without the Base44 infrastructure or Hugging Face backend.

The AI classification model can run entirely offline and runs on with the phone's hardware.
